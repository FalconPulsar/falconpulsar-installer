<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <!-- Match the production tray project; do not mask missing source imports. -->
    <ImplicitUsings>disable</ImplicitUsings>
    <Nullable>disable</Nullable>
  </PropertyGroup>
  <ItemGroup>
    <!-- Exercise the production implementation without loading Windows Forms. -->
    <Compile Include="../../tray-app/ConfigBackup.cs" Link="ConfigBackup.cs" />
  </ItemGroup>
</Project>
